<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="me.sunlianhui.coinflow.modules.template.mapper.TemplateMapper">

    <select id="selectAll" resultType="me.sunlianhui.coinflow.modules.template.entity.TemplateEntity">
        SELECT * FROM cf_template
    </select>

    <select id="selectById" resultType="me.sunlianhui.coinflow.modules.template.entity.TemplateEntity">
        SELECT * FROM cf_template WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="me.sunlianhui.coinflow.modules.template.entity.TemplateEntity">
        INSERT INTO cf_template (id, user_id, account_book_id, asset_account_id, category_id, type, amount, remark, name, created_at, updated_at)
        VALUES (#{id}, #{userId}, #{accountBookId}, #{assetAccountId}, #{categoryId}, #{type}, #{amount}, #{remark}, #{name}, #{createdAt}, #{updatedAt})
    </insert>

    <update id="update" parameterType="me.sunlianhui.coinflow.modules.template.entity.TemplateEntity">
        UPDATE cf_template
        SET user_id = #{userId}, account_book_id = #{accountBookId}, asset_account_id = #{assetAccountId}, category_id = #{categoryId}, type = #{type}, amount = #{amount}, remark = #{remark}, name = #{name}, updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="java.lang.Long">
        DELETE FROM cf_template WHERE id = #{id}
    </delete>

</mapper>
